(executables
 ((names (gen))
  (flags (:standard -safe-string))
  (libraries (str compiler-libs.common caml))
  (link_flags (-linkall))
  (preprocess no_preprocessing)))

(ocamllex (mapper))


(jbuild_version 1)
